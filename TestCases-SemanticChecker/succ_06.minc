num main  ()
{
    num x  ;
    num y  ;
    bool z ;
    x <- 1;
    y <- 0;
    z <- x > 10;
    while ( (not z) and (y >= 0) )
    {
        y <- y + x;
        x <- x + 1;
        z <- x > 10;
    }
    print x;
    print y;
    return 1 ;
}
